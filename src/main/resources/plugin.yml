main: ${spigot.main}
name: ${spigot.name}
version: ${project.version}
description: "${spigot.description}"
prefix: ${spigot.prefix}
website: "${spigot.website}"
author: mfnalex
database: false
api-version: "1.13"
load: POSTWORLD

softdepend: []

permissions:
  elytracombatfix.reload: Allows to reload the config file

commands:
  elytracombatfix:
    description: Reloads the config file
    permission: elytracombatfix.reload
    usage: /<command>